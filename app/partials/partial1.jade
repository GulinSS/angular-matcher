.sandbox
  form
    .angular-matcher.input-xxlarge
      span.match(ng-repeat='filter in filters')
        span.close(ng-click="remove(filter)")
          i.icon-remove
        span(angular-matcher-match)
      auto-input.last(result="child", key="blank", suggestions="showList")