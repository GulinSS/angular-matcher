"use strict"

describe "angularMatcher.directives", ->
  describe "directives", ->
    xdescribe "angularMatcher:\n", ->
      it "exists", ->
        expect(true).toBe false

      describe "parameters in the scope", ->
        it "expects 'result' field as result of user interaction", ->
          expect(true).toBe false

        it "expects 'filter'", ->